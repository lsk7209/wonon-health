# Batch 110 — Phase A editor/PM contracts

This directory contains only planning artifacts for 50 proposed Phase B+ articles. It does not include drafts, publication actions, schedule edits, or site changes.

Run `node build-contracts.cjs` to deterministically rebuild `manifest.json`, `contracts.json`, and `title-contract-map.csv`, then run `node validate-contracts.cjs`.
