# d018-d034 independent contract validation

- Scope: 17 authored replacement contracts (`d018` through `d034`) only; no shared manifest, builder, site, or schedule file changed.
- Contract review: each row names a distinct reader job, situation, decision moment, bounded answer claim, nearest-corpus distinction, concrete artifact, five-part architecture, FAQ pair, CTA, and three direct evidence targets with an exact verification question.
- Template review: the 17 section sequences are topic-shaped rather than the batch's former reusable observation/record/escalation skeleton. Artifacts range from a liver-metabolic recheck sheet to a wearable-alert evidence card, OTC ingredient card, thyroid imaging history, and medication-fall event card.
- Corpus boundaries explicitly referenced in the contracts include `fatty-liver-visit-questions`, `fasting-glucose-result-context`, `pain-reliever-use-context-log`, `recurrent-numbness-body-diagram`, `menopause-lipid-trend-table`, `menopause-palpitations-separate-log`, `blood-pressure-log-measurement-conditions`, `sleep-apnea-solo-observation`, `new-wheezing-observation-sheet`, `night-waking-transfer-safety`, and `mood-function-signal-light`.

## Fresh source reachability (2026-08-12 KST)

Parallel `fetch(..., { redirect: 'follow', timeout: 20s })` was run against every source URL, followed by targeted rechecks after the stale URLs were replaced. No final URL returned 404 or redirected to unrelated content. The remaining 403/405 responses are direct, bot-protected/manual-browser-verification targets rather than falsely reported as content-fetched.

| Rows | HTTP 200 targets | Bot-protected direct targets |
| --- | --- | --- |
| d018-d019 | 6 | 0 |
| d020 | 2 | AHA OTC/high-blood-pressure page (403) |
| d021-d024 | 12 | 0 |
| d025 | 1 | NIH ODS B12 and NINDS peripheral-neuropathy pages (403) |
| d026 | 2 | AHA menopause-and-heart page (403) |
| d027 | 2 | AHA atrial-fibrillation page (403) |
| d028 | 2 | AHA home-BP-monitoring page (403) |
| d029 | 2 | AHA sleep-apnea-and-heart page (403) |
| d030-d031 | 6 | 0 |
| d032 | FDA benzodiazepine warning and CDC falls pages | NIA medicines-and-older-adults page (405) |
| d033 | 2 | 988 get-help page (403) |
| d034 | 2 | NIA menopause page (405) |

The 403/405 behavior is an access-policy response, not a dead-link or redirect finding. Before drafting claims from those ten protected sources, verify their named question in an authenticated/manual browser or substitute a fresh 200 primary source; do not infer text from the transport result.

## Phase-B enrichment

Each owned row now carries authored identity, keyword and intent fields, a three-to-five route plan, source interpretation, article-specific detail ledger, research/outcome seed, three-action YMYL boundary, and three historical comparison objects. These fields are row-owned input for `merge-row-contracts.cjs`; the shared manifest remains unchanged.

## UTF-8 repair evidence

Fresh UTF-8 parse and raw scan: 17 rows, 51 source targets, `??` count 0, U+FFFD count 0, and every checked narrative field (`title`, `subtitle`, `reader_job`, `situation`, `answer_claim`, `source_interpretation`, `article_specific_detail`) contains Hangul.
